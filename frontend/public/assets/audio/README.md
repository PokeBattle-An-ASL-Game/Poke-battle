# Optional narrated cues — no files generated yet

Draft prompts live in `shared/voice-lines.json`. Only add a file/manifest once generated with actual ElevenLabs credentials in a private build process and approved under the relevant voice/content license. Audio is optional and off by default; captions and visual cues are always required. No API key belongs in this folder or browser.
